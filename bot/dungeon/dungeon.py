import discord
from discord.ext import commands
import json
import random

async def dungeon_handle(message):

    return

async def start_dungeon(player, dungeon):
    # Update Player Json Data to have "In Dungeon" in it
    return

async def resume_dungeon(player, dungeon):

    return

async def choice_display(player, dungeon):

    return

async def encounter_start(player, dungeon):

    return