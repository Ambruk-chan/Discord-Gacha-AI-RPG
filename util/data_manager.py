
from models import Ability
from models import Player
from models import Dungeon
from models import Enemy

async def read_character_data(display_name:str):
    player = Player()
    #read json and puts it into Player

    return player

async def write_character_data(player:Player):

    return 

async def read_dungeon_data(dungeon_name):
    dungeon = Dungeon()
    return dungeon

async def write_dungeon_data(dungeon:Dungeon):
    
    return 

async def read_enemy_data(enemy_name):
    enemy = Enemy()
    return enemy

async def write_enemy_data(enemy:Enemy):
    
    return

async def read_ability_data(ability_name):
    ability = Ability()

    return ability